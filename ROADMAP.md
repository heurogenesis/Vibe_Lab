# Vibe Lab — 예정 작업과 리소스 계획

> 계획 기준일: 2026-09-12. 사용자 목표일: 2026-09-15.
> 최신 진행 상태는 `PROGRESS.md`, 협업 규칙은 `AGENTS.md`에서 관리합니다.

## 1. 목표와 범위

**9월 15일 결과물:** 다른 이공계 전공의 프로필을 설정하고, 직무에 맞는 데이터 실습을 선택한 뒤, TypeScript 코드의 빈칸을 수정하여 콘솔·테스트·원리를 확인하는 로컬 MVP와 시연 자료.

교육 과정의 포트폴리오를 만든 후 SoftBank Hackathon 및 「모두의 창업 2기」 제출을 준비합니다. 공식 제출 일정·자격·요구 형식은 별도로 확인해야 하며 9월 15일을 해당 행사의 공식 마감일로 간주하지 않습니다.

초기에는 전자·기계·화학의 대표 흐름과 생명·바이오의 공개 데이터 예제를 검증합니다. 기존 44개 실습 변형을 유지하되, 마감 전 자료 수 확대보다 실행 안정성과 이해 가능한 설명에 시간을 씁니다.

**완료에 필요한 최소 흐름:** 프로필 저장 → 실제로 다른 학습 목표·데이터 추천 → 빈칸 코드 실행 → 실패 원인 확인 → 수정 후 통과 → 학습 요약 저장·재조회.

**MVP 범위:** 코드 셀, 콘솔, 고정 테스트, 단계별 힌트, 전공·직무별 추천, 자료실 검색·선택, 외부 데이터 1종 이상, 학습 기록.

**후속 범위:** 자유 형식 LLM 코드 피드백, 코드 임시 저장, 전문 도메인 심화, 추가 데이터 출처, 사용자 인증, AWS 평가 배포. LLM은 실제 키의 계약이 확인되고 기본 흐름이 안정된 경우에만 마감 전 추가합니다.

## 2. 일정과 우선순위

| 목표일 | 우선 작업 | 담당 | 완료 기준 |
|---|---|---|---|
| 9/12 | 협업 문서·세이브 포인트·브랜치 공유 | GPT / 사용자 | Claude가 동일 커밋과 미완료 항목을 확인할 수 있음 |
| 9/13 | 브라우저 실행·중단·외부 데이터 검증과 필요한 수정 | Claude | P0-01~03의 핵심 검증 결과 기록 |
| 9/14 | PostgreSQL 검증·회귀 검사·시연 범위 고정 | Claude / GPT | P0-04~05 완료 또는 제한을 명확히 기록 |
| 9/15 | 최종 로컬 시연·영상·포트폴리오 정리 | 사용자 / GPT | 새 환경 실행 안내와 대표 시나리오 설명 가능 |
| 평가 일정 확정 후 | EC2/RDS 평가 배포 및 종료 후 정리 | Claude / 사용자 | CLOUD-01~04 완료 |

9월 15일의 정확한 제출 시각과 AWS 평가 기간은 미정입니다. 일정이 부족하면 신규 기능을 미루고 실행·저장·시연 흐름을 우선합니다.

## 3. 다음 구현·검증 작업

각 항목은 하나 이상의 작은 작업으로 나눕니다. 실제 수정은 한 작업당 1~2개 파일이며, 예상 범위에 설정 파일이 포함되면 AGENTS의 승인 규칙을 따릅니다.

| ID | 작업 | 의존성 | 완료 조건 / 예상 시작 파일 |
|---|---|---|---|
| P0-01 | 실행·중단·오류 검증 | 세이브 포인트 | 정상 코드, 문법 오류, 실행 오류, 무한 반복 5초 제한, 중지, 화면 이탈을 실제 브라우저에서 확인. `src/runner.ts`, `src/CodeLab.tsx` |
| P0-02 | 실행 경계 검증 | P0-01 | 학습 코드의 네트워크·앱 데이터 접근 제한과 프로덕션 CSP를 확인. 실패 시 재현과 수정 후 결과 기록. `server/sandbox.ts`, `server/app.ts` |
| P0-03 | 외부 데이터 흐름 검증 | P0-01 | 실제 GitHub 조회·캐시·타임아웃·필드 변경 오류·명시적 샘플 전환 확인. 원자료가 학습 기록 API로 전송되지 않음을 확인. `src/data-loader.ts`, `src/CodeLab.tsx` |
| P0-04 | DB와 화면 상태 일관성 | P0-01 | 테스트 PostgreSQL에서 마이그레이션·저장·재조회 통과. 자료실/과제의 기록 표시 갱신 확인. 먼저 관련 코드를 읽고 수정 파일 1~2개 선택 |
| P0-05 | 추천과 시연 경로 검증 | P0-01~04 | 전자·기계·화학의 목표·단위·테스트 차이, 기존 프로필 호환, 알 수 없는 전공 안내 확인. `shared/catalog.ts`, `server/practice-curriculum.ts` |
| DOC-01 | 사용자 실행 안내·API 문서 갱신 | 검증 결과 | `README.md`, `docs/API.md`에 코드 셀·실행 요약 API·제약 반영 |
| DEMO-01 | 포트폴리오 증거 만들기 | P0 / DOC | 3~5분 데모, 구조도, 테스트 결과, 비용 판단, 알려진 한계 정리. 행사별 제출 규격은 사용자 확인 |
| P1-01 | 코드 임시 저장·자료실 사용성 | P0 | 화면 이동 시 코드 보관 정책을 정하고 과거 테스트 결과를 최신 코드 결과로 오인하지 않게 구현 |
| P1-02 | LLM 피드백 | 공급자 확인 + P0 | 서버 비밀 관리, 선택한 API 계약 테스트, 실제 테스트 결과 기반 설명, 오류·한도·사용량 표시. 모델 호출 실패가 코드 실행을 막지 않음 |
| P1-03 | 전공별 실제 원본 확대 | P0-03 | 원본 출처·라이선스·버전·크기·필드·학습 의미를 검수하고 데이터셋 단위로 추가 |
| PZ-01~06 | 프로필 조합별 Brave Search + 생성형 AI 개인화 자료 생성 (신규 트랙) | P0-05 | 세부 Step·비용·일정은 `docs/PERSONALIZATION_PLAN.md` 참고 |

## 4. 리소스와 비용 최적화

| 리소스 | 사용 계획 | 비용·범위 관리 |
|---|---|---|
| 사용자 | 요구사항, 키·AWS 계정, 시연·병합 승인 | 실제 API 키를 문서나 대화로 전달하지 않음 |
| GPT / Codex | 설계·문서·리뷰 | 구현 담당과 동시에 동일 파일 수정하지 않음 |
| Claude Code | 로컬 코드·테스트·배포 구현 | GitHub의 작업 브랜치를 기준으로 작은 단위 검증 |
| 로컬 PC | Node/VS Code/Chrome, PostgreSQL 또는 Docker | 현재 환경 재사용. 실제 DB 검증을 생략한 채 RDS 호환 완료로 표시하지 않음 |
| 학습 코드 실행 | 학습자 브라우저 | 서버 실행 워커·사용자별 컨테이너를 초기 MVP에 도입하지 않음 |
| 원자료 | GitHub 공개 원본/합성 샘플, 추후 읽기 API | 버전 고정·크기 제한·브라우저 캐시. 대용량 원자료 DB 복제 최소화 |
| 학습 상태 | 프로필·과제·진도·테스트 요약 | 최근 실행 요약 100건 제한. 공개 배포 전에 사용자별 분리 |
| LLM | 선결제된 API 사용 계정 | 현재 호출 없이 검증. 추후 실행 후 또는 명시적 요청 시 호출하고 매 타이핑마다 호출하지 않음 |

AWS 예산은 로컬 개발 **0원**, 평가 **2~3일 1만~2만 원**, **1주 2만~3만 원**, **1개월 7만~10만 원** 수준으로 잡습니다. 서울 리전 소형 EC2/RDS, 소량 트래픽, 평가 종료 후 리소스 삭제 기준입니다. LLM·도메인·추가 ALB/NAT·Multi-AZ 비용은 별도이며 정확한 가정과 공식 가격 출처는 `docs/MVP_ENGINEERING.md`를 참고합니다.

외부 DB를 사용해도 비용이 사라지는 것은 아닙니다. 공개 원자료의 외부 조회와 사용자 학습 상태의 영구 저장을 구분하여 관리합니다.

## 5. AWS 평가 배포와 이후 확장

**평가용 제안 구성:** 단일 EC2의 HTTPS 리버스 프록시·빌드된 React·Express, 외부 공개하지 않는 Single-AZ RDS PostgreSQL. 코드 실행은 계속 브라우저에서 수행합니다. 인스턴스 종류는 초기 소규모 부하 측정 후 확정합니다.

| ID | 단계 | 완료 조건 |
|---|---|---|
| CLOUD-01 | 배포 설계·승인 | 평가 기간·인원·예산, 인증 방식, VPC/보안 그룹, HTTPS, RDS TLS·비밀 주입 계획 확정 |
| CLOUD-02 | 재현 가능한 배포 | IaC 또는 명확한 생성 절차, 동일 빌드의 자동 배포·롤백. 공개 RDS·브라우저 비밀 노출 없이 접속 가능 |
| CLOUD-03 | 운영 검증 | 사용자별 데이터 분리, 오류 로그·알람, 백업 복구, 소규모 부하·LLM 사용량·비용 확인 |
| CLOUD-04 | 평가·정리 | 데모와 관측 결과 보관, 승인된 데이터 백업, EC2/RDS·디스크·IP·스냅샷 잔존 비용 확인 후 정리 |

현재 `server/index.ts`의 로컬 바인딩과 `server/app.ts`의 Host/Origin 제한을 해제하는 것만으로 배포 준비가 완료되는 것은 아닙니다. 인증·저장 구조·프록시·TLS 정책을 함께 설계하고 승인받습니다.

사용자 증가가 측정되면 앱 인스턴스와 DB 연결 풀, 캐시, 학습 기록 테이블 분리를 먼저 검토합니다. ALB·다중 인스턴스·Multi-AZ는 가용성 요구와 비용 근거가 있을 때 추가합니다. 셸/Python 커널은 별도 실행 서비스로 분리하는 후속 프로젝트이며 현재 브라우저 실행기에 섞지 않습니다.

## 6. 사용자에게 필요한 결정

1. **9월 15일 평가:** 정확한 시간, 필수 시연 기능, 발표/영상 제출 형식.
2. **LLM 연결 시점:** 실제 API 공급자·모델·엔드포인트·사용 한도. 키 자체는 제외.
3. **AWS 평가 전:** 평가 기간, 예상 동시 사용자, 예산 상한, 도메인 유무.
4. **외부 제출 전:** Hackathon/창업 프로그램 공식 공고, 공개 저장소·영상·개인정보 요구 사항.

이 결정이 없어도 P0 로컬 검증과 문서 정리는 진행할 수 있습니다. AWS 생성이나 미확인 API 규격 구현은 의존 정보가 확인된 뒤 수행합니다.

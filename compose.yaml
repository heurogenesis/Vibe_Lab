services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: vibelab
      POSTGRES_USER: vibelab
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vibelab_local}
    ports: ["127.0.0.1:5432:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vibelab -d vibelab"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  postgres_data:
